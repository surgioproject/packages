surgio core path /Users/yihang/Development/projj/github.com/geekdada/surgio

Compared with `master`/`v3` at `v3.19.0`, the current `v4` branch contains **13 commits across 213 files**, with **7,886 additions and 12,223 deletions**.

**Completed Work**

1. **Raised runtime requirements** (`a8790a1`)
   - Minimum Node.js moved from 18 to `22.22.2`.
   - Development version moved to Node 26.
   - CI now tests Node 22 and 26 on Ubuntu and macOS.
   - `doctor` reliably reports the active Node executable and version.

2. **Modernized pnpm and workspace setup** (`a8790a1`, `9fdd94b`)
   - Migrated from pnpm 10 to `pnpm 11.21.0`.
   - CI uses `pnpm/action-setup` and frozen lockfile installs.
   - Added explicit VuePress dependency overrides and native build permissions.

3. **Expanded CI coverage** (`d020440`)
   - CI now runs build, type checking, linting, CLI tests, coverage, and documentation builds.
   - Added package-output and TypeScript-consumer fixtures.
   - Added checks for both the configured TypeScript 6 and TypeScript 7/native compiler paths.
   - Added regression coverage for loading CommonJS providers.

4. **Removed generated editor configuration** (`37a94c8`)
   - Deleted the tracked autogenerated `.vscode/settings.json`.

5. **Upgraded the CLI to oclif 4** (`b8ca0e8`)
   - Upgraded `@oclif/core`, `@oclif/plugin-help`, and `@oclif/test`.
   - Migrated production and development entrypoints to `oclif.execute()`.
   - Migrated CLI tests to `runCommand()`.
   - Added command-help, production-entrypoint, and clean-error-output tests.
   - **Breaking:** removed `surgio plugins` and third-party oclif plugin management.

6. **Migrated all tests to Vitest 4** (`6c81019`, `6cc54b8`)
   - Removed AVA, Mocha, Chai snapshot integration, nyc, and Sinon.
   - Added independent unit and CLI Vitest configurations.
   - Migrated snapshots to native Vitest `.snap` files.
   - Migrated mocks, spies, environment overrides, and lifecycle hooks to `vi`.
   - Preserved Istanbul exclusions through esbuild using `@preserve`.
   - Updated repository instructions and coverage commands.

7. **Modernized major dependencies** (`617efaf`, `72472c8`)
   - Upgraded Zod 4, ESLint 10, Prettier 3, Husky 9, ioredis 6, cache-manager 7, and associated tooling.
   - Migrated caching to CacheManager + Keyv/Cacheable, including Redis support.
   - Upgraded ESM-only dependencies including Got 15, Execa 10, Inquirer 14, Chalk 6, Ora 9, and Babel Parser 8.
   - Updated source code for changed dependency APIs.
   - Restored commit-message and pre-commit hooks under modern Husky.

8. **Added general S3-compatible uploads** (`4324288`)
   - Replaced the Alibaba-only upload implementation with MinIO’s S3-compatible client.
   - Added dedicated backends for Alibaba OSS, Cloudflare R2, and generic S3 services.
   - Added public, internal, and accelerated OSS endpoints.
   - Added R2 EU and FedRAMP jurisdiction endpoints.
   - Added path-style and virtual-hosted-style S3 support.
   - Added environment-based credentials with config fallback.
   - Added strict endpoint and backend-specific validation.
   - Upload now synchronizes the complete prefix, deletes stale objects, handles arbitrary listing sizes, and reports deletion failures.
   - Added extensive unit tests and configuration documentation.

9. **Converted the published package to ESM** (`bdccfab`)
   - Changed the package to `"type": "module"` and NodeNext module resolution.
   - Converted internal imports to explicit ESM paths.
   - Added an exports map for root and supported subpath entrypoints.
   - Removed legacy root-level JavaScript and declaration wrappers.
   - Preserved synchronous CommonJS consumption through `module-sync` exports.
   - Preserved existing CommonJS `surgio.conf.js` and provider files via a `createRequire` loader.
   - Added tests for ESM imports, CommonJS `require()`, emitted artifacts, and TypeScript consumers.
   - Added local CommonJS package scopes for CLI, examples, templates, and fixtures.

10. **Removed BlackSSL support** (`259cdb4`)
    - Deleted `BlackSSLProvider`.
    - Removed its provider enum, configuration type, helper, exports, dispatch, and snapshots.
    - This is another v4 compatibility break.

11. **Made Mihomo the default Clash core** (`21227a9`)
    - Default `clashCore` changed from legacy `clash` to `clash.meta`.
    - Added `mihomo` as an accepted alias normalized to `clash.meta`.
    - Updated node-level and template-level fallback behavior.
    - Kept explicit `clash` and `stash` behavior intact.
    - Updated input types so transformed configuration values remain type-safe.
    - Added regression tests and updated documentation.

**Branch State**

`origin/v4` currently ends at the object-storage commit. These four accomplishments remain local and unpushed:

- ESM package conversion
- BlackSSL removal
- Final ESM dependency upgrades
- Mihomo default

The worktree is clean. I inspected the branch history and diffs for this report; I did not rerun the complete test suite.